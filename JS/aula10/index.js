// String, number, undefined, null, boolean, symbol
const nome = 'Diego' // string
const nome1 = "Diego" // string
const nome2 = `Diego` // string
const num1 = 10 // number
const num2 = 10.52 // number
let nomeAluno // undefined -> não aponta para o local nenhuma na memória
let sobrenomeAluno = null // Nulo -> não aponta para local nenhuma na memória
const aprovado = true // boolean = true, false (lógico)


console.log(typeof nome, nome)

